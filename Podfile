# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CashUTask' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CashUTask
  pod 'Alamofire', '~> 5.2'
  pod 'SDWebImage', '~> 5.0'
  pod 'SwiftyJSON', '~> 4.0'
  target 'CashUTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CashUTaskUITests' do
    # Pods for testing
  end

end
